# NeoMua repository instructions

本文件适用于整个仓库。更深目录若新增 `AGENTS.md`，只能补充模块约束，不能降低这里的安全、验证和可追溯要求。

## 1. 正常解决与安全边界

- 优先通过项目正常机制解决问题。正常途径无法解决、同一阻塞连续尝试仍失败，或需要降级、绕过检查、扩大权限、高风险迁移时，立即停止并向用户说明。
- 保护工作区已有改动。不得使用 `git reset --hard`、强推、擅自删除分支或文件，也不得用覆盖式操作清理不属于当前任务的变化。
- 未经用户明确授权，不推送远端、不合并默认分支、不发布 Release、不部署，也不修改 staging/production 主机或正式数据。
- 每次修改前先核实当前分支、工作区、相关代码、测试、context 和外部依赖事实；不能把计划、假设或未验证状态写成已实现事实。

## 2. 变更风险分级

开始实现前声明本次变更为 S、M 或 H；不确定时选择更高等级。路径检查只能给出最低等级，不能替代语义判断。

### S — 低风险微调

适用于不改变产品语义、数据、权限或运行拓扑的局部调整，例如文案、注释、格式、测试描述、无行为变化的重构。

完成要求：

- 运行相关格式、静态检查；
- 运行能覆盖改动点的最小相关测试；纯文档可用文档/YAML/链接等检查，并说明未运行应用测试的原因；
- 记录 `context impact`，通常为 `none`，并说明为什么没有语义能力变化。

### M — 普通功能或局部行为变化

适用于单模块功能、普通缺陷修复、非敏感 API/UI 行为、开发工具和工程流程调整。

完成要求：

- 运行受影响的单元、集成或端到端测试；
- API 变化时验证 OpenAPI 与生成客户端；
- 明确 `context impact`；只有当前语义能力、接口、流程或页面事实变化时才更新对应 context；
- 说明范围内与明确不在范围内的内容。

### H — 高风险变化

以下变化至少为 H：认证、会话、权限、安全边界、数据库模型或迁移、依赖或锁文件、部署与工作流、运行时拓扑、跨模块架构、不可逆数据操作。

完成要求：

- 运行完整相关测试集以及仓库级检查；
- 验证迁移与回滚/恢复方案；数据库迁移不得假设自动可逆；
- 在 staging 验证健康检查和关键冒烟流程；
- 记录实际测试、迁移、回滚和 staging 证据。外部环境或权限缺失时，准备清单后停止，状态不得标记为完成。

## 3. Context impact

每个任务和 PR 都必须记录 `context impact`：

- `none`：没有改变当前语义能力，并写明理由；
- `updated`：语义能力或当前事实发生变化，列出更新的 context 文件。

不要为了通过检查机械更新 context。仅测试、格式、内部重构或等价实现通常不更新。

需要更新时按 `context/readme.md` 路由：

- 全局事实写根级文件；
- 模块接口、流程和 UI 写 `context/modules/`；
- 版本需求和变更快照写 `context/prds/`、`context/changelogs/`；
- 只记录已实现并已验证的事实。

## 4. 分支、提交与版本

- 当前分支与任务目标一致时继续；否则从已验证起点创建 `feat/*`、`fix/*` 或 `chore/*` 短分支。
- 默认分支必须始终保持可发布，不在默认分支堆积未验证工作。
- 每个提交只覆盖一个可说明目标；提交前运行与风险等级匹配的验证。
- 获得远端写入授权后，在首个稳定提交和工作结束时推送；未获授权时保留本地提交并明确报告未推送。
- 合并后只删除已经确认合并且获得删除授权的短分支。
- 版本通过 tag/GitHub Release 表示，不再新增长期版本开发分支。

## 5. 完成定义

只有同时满足以下条件才可声明完成：

- 范围已收敛，未把无关问题混入当前任务；
- 已按 S/M/H 运行并记录测试；
- 已记录 `context impact`，需要时更新了正确的 context；
- 已创建逻辑提交，或明确说明为什么尚不能提交；
- 分支、提交、验证、未推送/未部署状态可追溯；
- 剩余风险、外部配置和下一步明确。

使用 `scripts/check-change-process.sh`（或 `make check-change`）检查最低风险等级与证据字段。脚本通过不代表可以降低人工判断出的风险。
