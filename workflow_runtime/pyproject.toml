[project]
name = "neomua-workflow-runtime"
version = "0.6.0"
requires-python = ">=3.10,<4.0"

[dependency-groups]
dev = ["pytest>=7.4,<8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["workflow_runtime"]
