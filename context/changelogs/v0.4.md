# v0.4 变更记录（已完成）

- 新增 namespace 独立的平台/节点运行时配置和 Anthropic-compatible Model Gateway。
- 新增独立 Claude Agent SDK runtime-worker、多轮测试与完整过程持久化。
- 新增一次性节点注册、Ed25519 设备身份、WSS 心跳、凭证轮换和长期离线恢复。
- 新增可靠 Agent 任务状态机、租约、幂等调度、SQLite 事件 spool、取消和显式 retry。
- 新增不可变签名 Agent/Skill/MCP/CLI/工作区内容、有效期发布、节点原子应用和回滚数据链。
- 新增运行时管理、节点、任务、制品与发布页面。
- 发布详情支持 Admin 显式重试失败部署和创建可审计回滚发布，Developer 保持只读。
- OpenAPI 生成器升级至 0.99，迁移到官方 Axios/TypeScript/SDK 插件并清除 npm 已知漏洞。

已完成 Compose、Docker、迁移、Python、前端构建和 Playwright 完整回归验收。
