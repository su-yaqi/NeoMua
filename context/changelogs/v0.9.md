# v0.9 变更记录（整改完成，尚未合并）

## 基本信息

- 开发快照时间：2026-07-17
- 对应 PRD：`context/prds/v0.9/`
- 数据库 head：`fc5a7b9d1e34`
- 开发分支：`codex/v0.9`
- 状态：需求一致性与安全整改复核通过；按用户要求尚未合并到主分支

## 实现内容

- 将执行位置与执行引擎拆分为 Runtime Node 和 Runtime Instance。一台 Node 可承载多个 Claude Code、Codex 等 Runtime Instance；项目默认运行时、Skill 同步和 MCP 目标统一指向 Runtime Instance。
- Harness 收敛为 Runtime 内部引擎适配器，不再作为 Agent 下可写的独立配置资源。旧 Harness 与 Runtime Profile 数据仅保留只读历史语义，旧内部能力接口明确返回不可用，不参与新任务执行。
- Runtime Instance 使用不可变配置修订，记录引擎类型、受控配置、配置摘要和应用状态；Node Runtime 按适配器生成确定性启动参数，并按实例报告能力、配置应用和在线状态。
- 新增稳定模型定义、Runtime 模型能力报告与经过验证的模型绑定。只有存在明确 provider/runtime-native 路由且验证通过的模型才可用于执行；模型身份、路由与能力指纹均进入执行证据。
- Agent 草稿与 Release 增加模型偏好和引擎中立执行策略。Release 不绑定 Harness 或具体 Runtime；激活前按目标 Runtime Instance 执行服务器预检，成功后异步部署并生成 Runtime Agent Release。
- Conversation、Agent 组与 Workflow 支持选择具体模型或 `agent_preference`。后者只允许解析为同一稳定模型身份的可用绑定，不进行相似名称、同系列或跨路由替代。
- 项目默认 Runtime、配置修订、Activation 和 Task 均统一使用 Runtime Instance。任务开始前冻结 Runtime、引擎、模型、路由、选择来源和能力指纹；后续配置变化不会改写历史执行。
- 删除旧 Runtime Profile/Harness 的前端写入入口和测试功能，新增 Runtime Instance 列表、实例配置修订、模型能力与绑定管理界面；Developer 保持只读，Admin 才能执行配置与绑定操作。
- 新增 v0.9 Alembic 迁移，完成运行时/模型/执行结构、目标外键、逐次模型调用用量及 JSONB/索引元数据对齐。旧模型迁移只接受可证明的自动发现记录；未知、手工、过期或歧义身份会阻断升级并要求 Admin 确认。

## 审阅整改

- Runtime Worker 与 Node Runtime 使用本地持久化 applied 配置、实际 Adapter、能力缓存和已验证模型路由生成模型证据；控制面精确核对后才签发首次模型调用凭据，不再接受快照自证。
- Runtime 配置在应用边界执行类型校验、环境清洗、工作目录、权限、Tool/MCP、能力、超时和模型路由约束。配置环境变量还须通过运维级 `NEOMUA_RUNTIME_ENV_ALLOWLIST`；无法可靠执行的隔离、restricted network、CPU、内存、并发或未知策略明确失败。
- Node 断连、能力报告过期、配置/能力指纹变化、Provider 或模型停用、Binding 验证到期都会使 Binding 从可用目录和预检中失效；平台与 Node 均周期上报本地能力事实。
- 绑定 Agent Release 的直接任务固定使用 Release system prompt；任务创建、调度和接收阶段均复核冻结策略、能力、目录根、超时和当前 Binding。
- 新增 Release compatibility API、逐次模型调用用量表/API/UI；会话参与者改以实例 ID 区分，允许同一 Agent/Release 承担多个角色。
- Runtime 配置 CAS 与 Binding validation attempt 使用行锁和确定性冲突响应，避免并发竞争泄漏为数据库 500。

## 验证结果

- 本轮复核确认 Backend 全量 Pytest 213 项、Runtime Worker 19 项、Node Runtime 27 项和 Playwright 74 项通过。
- 前端 TypeScript/Vite production build、Python Ruff、变更范围 Biome 与 `git diff --check` 通过。
- 数据库已升级至 `fc5a7b9d1e34`，`alembic check` 确认无待生成操作。
- 新增 Runtime 本地证据不一致、伪造证据、应用 secret 环境泄漏、Node 断连/过期能力、Binding 失效、并发与冻结行为等负向测试。

完整问题闭环、边界说明与验证证据见 `context/audit-2026-07-17-v0.9.md`。

## 已知非阻断项

- 本地开发 `.env` 仍使用模板占位密钥，测试日志会提示弱密钥；生产或共享环境必须注入独立安全密钥。
- Playwright Node 加载器仍输出 `module.register()` deprecation warning，且 `FORCE_COLOR` 会覆盖 `NO_COLOR`；不影响 74 项验收结果。
