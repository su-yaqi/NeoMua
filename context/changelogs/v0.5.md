# v0.5 变更记录（已完成）

## 基本信息

- 完成时间：2026-07-13
- 对应 PRD：`context/prds/v0.5/`
- 数据库 head：`3c1717dcaceb`

## 实现内容

- 新增 Agent/Harness CAS 草稿、目标能力报告、统一校验和复制/归档约束。
- 新增声明式 Skill 安全扫描与不可变 Version、内置/MCP Tool 目录及只可收紧策略。
- 新增 MCP Server/Revision/Target、平台密文、节点本地 `secret_ref`、目标校验、Tool 快照、Runtime Manager 和 stale 指纹链路。
- 新增声明式 Plugin 草稿、依赖冲突检查、精确 lock、签名 Version 和 deprecate。
- 新增 canonical ResolvedAgentSpec 与 Claude Adapter，发布/激活/任务共享同一 digest 和物化规则。
- 新增签名 Agent Release、服务端 activation precheck、逐目标 deployment、partial、显式 retry/rollback 和 Runtime Agent 矩阵。
- 新增冻结 Release 的 Session/Task、显式 Skill invocation 与绑定 args digest 的 Tool Approval。
- 新增 `neomua` Operator CLI：Keychain refresh session、正式 API mutation、幂等重试、dry-run、JSON/退出码和节点本地 secret 命令。
- 完成 Agent、Skill、MCP、Plugin、Release、Activation 与节点 MCP 就绪状态页面；修复嵌套详情路由被列表页遮蔽的问题。

## 验证结果

- clean DB 从 base 到 Alembic head 迁移通过。
- Backend：209 passed；Runtime Worker：13 passed；Model Gateway：10 passed；Node Runtime：24 passed；Operator CLI：10 passed。
- Frontend lint、TypeScript 与 production build 通过；完整 Playwright 回归 67 项通过，其中包含 Agent 管理关键路径 7 项。
- 新增正式 API 全流程覆盖 Skill → MCP 校验/Tool 快照 → Plugin → Agent Release → Activation precheck/activate。

## 实现期修复

- Activation target 校验按 Tool 来源区分 Claude 内置 Tool 与 MCP Tool，避免合法 MCP Release 被误判不兼容。
- 节点心跳只上报 MCP secret ref/fingerprint；变化或移除会使 target stale，不上传 value。
- Operator CLI 发布/激活 dry-run 调用服务端正式 validate/precheck，批次 partial 使用独立退出码 8。

## 已知非阻断项

- Vite 仍提示主入口 chunk 超过 500 kB；不影响本版本功能和构建结果，后续可按页面继续拆包。
- 本地 `.env` 使用开发占位密钥时测试会产生弱密钥 warning；生产/预发布继续由配置启动校验拒绝弱密钥。
