# v0.6 变更记录（已完成）

## 基本信息

- 完成时间：2026-07-13
- 对应 PRD：`context/prds/v0.6/`
- 数据库 head：`e1b9c8d7f603`
- 开发库处置：已在备份后受控重建；备份为 `.backups/neomua-app-before-v06-rebuild-20260713.dump`，SHA-256 为 `a5c079b67fe27534746b81bb5ff7290e83362f5c85e98c19d78339a5415fb24f`

## 实现内容

- 新增项目、无主次多仓库、Runtime 工作区证明、多个 Spec 位置、不可变标准版本和精确绑定/差异预览。
- 新增固定模型 Chat 与固定 Agent Release 会话、单/多 Agent 圆桌、点名/全体消息、真实协作者委派、完整委派审计和追加式项目上下文快照。
- 会话消息与委派变化写入 `conversation_event`，提供支持 `Last-Event-ID` 的可恢复 SSE；前端不再轮询主线消息。
- 新增 Workflow SDK、严格 Package/Manifest/DAG/Schema/前端摘要校验、注册表、namespace 启用与独立应用路由。
- Workflow 应用按任务固定的 `component_key` 解析。开发库同时保留 `project-delivery` 1.0.2 与 1.0.3；未知组件明确阻断，不以通用页面降级替代。
- 新增持久化 `runtime_job`，让仓库探测、Workflow Validator 和 Handler 在平台或节点 Runtime 上通过租约、幂等与持久状态执行，不依赖 FastAPI 进程内任务。
- 新增持久化项目任务状态机、Runtime/Agent/仓库预检、节点修订、执行轮次、Gate、三种确认、跳过、更新传播、原 Agent 会话续跑和完成只读。
- Workflow 任务页补齐固定版本、上下文快照、节点历史、Gate、确认、错误、显式重试、外部状态证明、持续 Agent 会话、事件流和严格扫描附件。
- 有副作用节点缺失完成证明时进入人工处理；只有具名外部状态证据证明未执行或已补偿后可显式重试，不 force、不换 Runtime、不静默降级。
- 修正项目、会话和 Workflow 任务详情页的 TanStack 独立路由，使直接访问 URL 与站内导航一致。
- 生产前端按 React、TanStack、UI 和其余依赖分包，主入口由 735 KiB 降至约 86 KiB，构建不再产生 500 KiB chunk 告警。

## 验证结果

- Backend 全量 Pytest：226 项通过。
- Runtime Worker：14 项通过；Node Runtime：26 项通过；内置 Workflow Package：3 项通过。
- Python Ruff、Frontend Biome、TypeScript 与 Vite production build 通过。
- Alembic `check` 无模型差异，开发库位于 `e1b9c8d7f603 (head)`。
- Docker 完整开发栈构建并启动成功；数据库、后端、Model Gateway、Runtime Worker 均健康，前后端健康端点返回 200。
- Playwright 全量真实栈回归：71 项通过，包含 Workspace、Project、固定版本 Workflow 和禁止前端降级的 v0.6 验收。
- OpenAPI 前端客户端已重新生成；`project-delivery` 1.0.3 的磁盘前端摘要与数据库 `build_digest` 均为 `3ed37a82a37d6430bb20fe96f4fb794c6cb0725ef3ec2c197729c70d22fcb72c`。

## 已知非阻断项

- 本地开发 `.env` 仍使用模板占位密钥，因此测试与启动日志会给出弱密钥 warning；生产或共享环境必须注入安全密钥。
- Playwright 使用的 Node 加载器会输出 `module.register()` deprecation warning；不影响当前 71 项验收结果，后续随测试工具链升级处理。
