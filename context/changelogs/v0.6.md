# v0.6 变更记录（已完成）

## 基本信息

- 完成时间：2026-07-13
- 对应 PRD：`context/prds/v0.6/`
- 数据库 head：`b5876b129fbd`

## 实现内容

- 新增项目、无主次多仓库、Runtime 工作区证明、多个 Spec 位置、不可变标准版本和精确绑定/差异预览。
- 新增固定模型 Chat 与固定 Agent Release 会话、单/多 Agent 圆桌、点名/全体消息、委派审计和追加式项目上下文快照。
- 新增 Workflow SDK、严格 Package/Manifest/DAG/Schema 校验器、注册表、namespace 启用与独立应用路由。
- 新增持久化项目任务状态机、Runtime/Agent/仓库预检、节点修订、执行轮次、Gate、三种确认、跳过、更新传播、原 Agent 会话续跑和完成只读。
- 有副作用节点缺失完成证明时进入人工处理；只有具名外部状态证据证明未执行或已补偿后可显式重试，不 force、不换 Runtime、不静默降级。
- 新增 `project-delivery` 内置 Workflow Package、项目/工作台/任务/Workflow/Spec 管理页面和 V0.6 回归测试。

## 验证结果

- clean 隔离 PostgreSQL 的 `base → V0.6 → v0.5 → V0.6` 往返迁移通过，`alembic check` 无差异；downgrade 显式清理 V0.6 PostgreSQL enum。
- Backend 全量回归 215 项通过；新增 V0.6 模块测试 6 项通过。
- Frontend Biome lint、TypeScript 与 production build 通过。

## 已知非阻断项

- Vite 主入口 chunk 仍超过 500 kB，与 v0.5 相同；不影响构建结果。
- 开发 `.env` 的占位密钥会产生弱密钥 warning；部署环境仍需使用安全配置。
